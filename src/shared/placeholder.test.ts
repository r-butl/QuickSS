import { describe, expect, it } from 'vitest'

// Placeholder test to prove the Vitest runner is wired up end-to-end.
// Delete this file once real tests exist for src/shared (Task 2 onward).
describe('placeholder', () => {
  it('proves the test runner works', () => {
    expect(1 + 1).toBe(2)
  })
})
