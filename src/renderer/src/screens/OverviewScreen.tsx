import AppHeader from '../components/AppHeader'

/**
 * Placeholder overview-mode screen. Real overview functionality is built in
 * Phase 6 - this just proves the navigation wiring works.
 */
function OverviewScreen(): React.JSX.Element {
  return (
    <div>
      <AppHeader />
      <main>
        <p>Overview mode - coming in Phase 6</p>
      </main>
    </div>
  )
}

export default OverviewScreen
