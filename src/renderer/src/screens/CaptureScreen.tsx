import AppHeader from '../components/AppHeader'

/**
 * Placeholder capture-mode screen. Real capture functionality is built in
 * Phase 3 - this just proves the navigation wiring works.
 */
function CaptureScreen(): React.JSX.Element {
  return (
    <div>
      <AppHeader />
      <main>
        <p>Capture mode - coming in Phase 3</p>
      </main>
    </div>
  )
}

export default CaptureScreen
